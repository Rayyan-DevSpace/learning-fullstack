const costCalculation = (quantity, price) => {
    let cost = quantity * price;
    
    // console.log(cost);

    return cost;
};

// costCalculation(13,100);
// costCalculation(5,50);

console.log(costCalculation(13,100));
